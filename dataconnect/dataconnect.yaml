specVersion: "v1beta"
serviceId: "vizion-backend"
location: "us-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vizion-backend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
