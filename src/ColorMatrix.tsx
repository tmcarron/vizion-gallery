import "./ColorMatrix.css";

const ColorMatrix = () => {
  // const [imgUrl, setImgUrl] = useState<string | null>(null);

  // const [baseColor, setBaseColor] = useState<string | null>(null);

  return <div className="ColorMatrix"></div>;
};

export default ColorMatrix;
